apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: immich
  namespace: immich
spec:
  ingressClassName: contour
  rules:
    - host: immich.example.com   # <-- ändern!
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: immich-server
                port:
                  number: 2283
  # TLS (optional, z.B. mit cert-manager)
  # tls:
  #   - hosts:
  #       - immich.example.com
  #     secretName: immich-tls
